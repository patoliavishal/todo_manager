name: todo_manager
description: A collaborative TODO sharing Flutter application with real-time updates.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.3
  freezed_annotation: ^3.1.0
  uuid: ^4.5.2
  share_plus: ^12.0.1
  firebase_core: ^4.2.1
  cloud_firestore: ^6.1.0
  firebase_auth: ^6.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.6
  freezed: ^3.2.3
  riverpod_generator: ^3.0.3
  json_serializable: ^6.11.1

flutter:
  uses-material-design: true
